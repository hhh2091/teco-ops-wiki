---
id: doc-perf-optimization-sdaa-c-v2-0-2-p02
title: "性能优化手册 SDAA C 篇 v2.0.2 — 第2部分 (ch4)"
type: source-doc-part
parent_doc: doc-perf-optimization-sdaa-c-v2-0-2
product_version: "v2.0.2"
source_file: "external/文档/性能优化手册-SDAA C篇_v2.0.2.pdf"
raw_text: "sources/docs/raw/性能优化手册-SDAA C篇_v2.0.2.txt"
raw_line_range: "3514-6945"
architectures: [sdaa, teco-t1]
confidence: verified
tags: [atomic, broadcast, math-functions, matmul, memory, perf-optimization, perf-sampling, programming-model, runtime, sdaa, simd-vectorization, teco-t1, tecocc, thread-group-sync]
---

# 性能优化手册 SDAA C 篇 v2.0.2（第2部分）

> 说明：本部分覆盖第 4 章「性能优化」的后半部分（接续第 1 部分）。本部分起始于 4.2.4.1「SPE ID 相关」的测试代码续接处。为保持代码示例完整，下文从该示例的 `perf_start()` 段落开始呈现。

## 4.2.4 Broadcast 数据搬运

### 4.2.4.1 SPE ID 相关（续）

本节介绍使用线程组进行 Broadcast 数据搬运的性能优化策略和示例。以下为「优化示例」测试代码的续接部分（接前一部分的 `broadcast_perf` 函数体）。

#### 测试代码（续）

```cpp
        perf_start();
        for (int i = 0; i < 1000; i++) {
             broadcast(&dst, &src, sizeof(int), 0, {0, 0}, 0, BroadcastSpmToSpm, handle);
        }
        perf_stop();
        perf_print();
  }


  // broadcast to 0, 1, 2, 3, 4, 5, 6, 7
  __global__ void test_a()
  {
        unsigned long thread_group[8] = {0, 1, 2, 3, 4, 5, 6, 7};
        ThreadGroup tg(thread_group, 8);
        broadcast_perf(tg);
  }


  // broadcast to 0, 1, 8, 9, 16, 17, 24, 25
  __global__ void test_b()
  {
        unsigned long thread_group[8] = {0, 1, 8, 9, 16, 17, 24, 25};
        ThreadGroup tg(thread_group, 8);
        broadcast_perf(tg);
  }


  // broadcast to 0, 1, 9, 10, 18, 19, 27, 28
  __global__ void test_c()
  {
        unsigned long thread_group[8] = {0, 1, 9, 10, 18, 19, 27, 28};
        ThreadGroup tg(thread_group, 8);
        broadcast_perf(tg);
  }
  int main()
  {
        sdaaSetDevice(2);
        test_a<<<1>>>();
        sdaaDeviceSynchronize();
        test_b<<<1>>>();
        sdaaDeviceSynchronize();
        test_c<<<1>>>();
        sdaaDeviceSynchronize();
        return 0;
  }
```

#### 优化效果

| 线程组中所有 SPE 的 ID | 平均每次 Broadcast 操作的执行周期（拍） |
|---|---|
| 0, 1, 2, 3, 4, 5, 6, 7 | 266 |
| 0, 1, 8, 9, 16, 17, 24, 25 | 322 |
| 0, 1, 9, 10, 18, 19, 27, 28 | 1362 |

在本示例中，满足优化策略的条件时使用线程组进行阻塞型 Broadcast 数据搬运的性能，是不满足条件时的约 5.12 倍。

### 4.2.4.2 非阻塞型 Broadcast 数据搬运

本文主要介绍非阻塞型 Broadcast 数据搬运的性能优化策略和示例。

#### 优化策略

如果搬运的数据与当前程序的后续操作不存在数据依赖时，使用非阻塞型接口（详情请参考《SDAA C 编程指南》的非阻塞型 Broadcast 数据搬运章节）可以更好的进行流水排布，相对阻塞式接口（详情请参考《SDAA C 编程指南》的阻塞型 Broadcast 数据搬运章节）性能更好。

#### 优化示例

在设备端使用阻塞型广播接口将 SPE0 中的数据广播到 SPE0~SPE7 内然后执行与数据搬运无数据依赖的计算，与使用非阻塞型数据搬运接口同步执行将 SPE0 中的数据广播到 SPE0~SPE7 的数据搬运操作和与数据搬运无数据依赖的计算进行性能测试并比较。

每个测试均调用 10000 次数据搬运和与搬运数据无依赖的计算操作，从而计算平均值以消除初次调用时 cache 影响。

#### 测试环境

| TecoDriver 和 TecoToolKit 版本 | 计算核心频率 | 数据搬运量 |
|---|---|---|
| v1.2.0 | 2.36GHz | 10KB |

#### 测试代码

```cpp
  #include<sdaa_perf.h>
  using namespace sdaa;


  #define SIZE 2560
  #define SIZE_CAL 256
  #define TIME 10000


  __device__ __attribute__((noinline)) void test_sync(int *dst, int *bc_dst, int *bc_src, int *d_cal)
  {
          // 设置线程组 SPE0~SPE7
          ThreadGroup thread_group(0xff);
          BroadcastHandle handle(&thread_group);
          // 初始化目的地址数据
          if (thread_group_is_included(thread_group, threadIdx)) {
              memset(bc_dst, 0, SIZE * sizeof(int));
          }


          intv16 cal_src;
          intv16 add = 1;
          // 执行SPE0~SPE7同步
          sync_threads(thread_group);


      if (thread_group_is_included(thread_group, threadIdx)) {
          // 统计性能数据
          perf_start();


          for (int time = 0; time < TIME; time++) {
              broadcast(bc_dst, bc_src, SIZE * sizeof(int), 0, {0, 0}, 0, BroadcastSpmToSpm, handle);


              // 异步进行与数据搬运无数据依赖的计算
              for (int idx = 0; idx < SIZE_CAL; idx += 16) {
                  // 使用向量进行计算
                  simd_load(cal_src, d_cal + (SIZE_CAL * threadIdx) + idx);
                  intv16 tmp = cal_src + add;
                  simd_store(tmp, d_cal + (SIZE_CAL * threadIdx) + idx);
              }
          }
          // 统计性能数据
          perf_stop();
      }


      // 将广播数据拷贝到SPE0~SPE7的目的地址
      if (thread_group_is_included(thread_group, threadIdx)) {
          memcpy(dst + threadIdx * SIZE, bc_dst, SIZE * sizeof(int));
      }
  }


  __device__ __attribute__((noinline)) void test_async(int *dst, int *bc_dst, int *bc_src, int *d_cal)
  {
      // 设置线程组 SPE0~SPE7
      ThreadGroup thread_group(0xff);
      BroadcastHandle handle(&thread_group);
      // 初始化目的地址数据
      if (thread_group_is_included(thread_group, threadIdx)) {
          memset(bc_dst, 0, SIZE * sizeof(int));
      }


      intv16 cal_src;
      intv16 add = 1;
      // 执行SPE0~SPE7同步
      sync_threads(thread_group);


      if (thread_group_is_included(thread_group, threadIdx)) {
          // 统计性能数据
          perf_start();


          for (int time = 0; time < TIME; time++) {
              // 发起非阻塞广播操作
              broadcast_async(bc_dst, bc_src, SIZE * sizeof(int), 0, {0, 0}, 0, BroadcastSpmToSpm,
  handle);


              // 同步进行与数据搬运无数据依赖的计算
              for (int idx = 0; idx < SIZE_CAL; idx += 16) {
                  // 使用向量进行计算
                  simd_load(cal_src, d_cal + (SIZE_CAL * threadIdx) + idx);
                  intv16 tmp = cal_src + add;
                  simd_store(tmp, d_cal + (SIZE_CAL * threadIdx) + idx);
              }
              // 等待非阻塞广播操作完成
              broadcast_wait(handle);
          }
          // 统计性能数据
          perf_stop();
      }


      // 将广播数据拷贝到SPE0~SPE7的目的地址
      if (thread_group_is_included(thread_group, threadIdx)) {
          memcpy(dst + threadIdx * SIZE, bc_dst, SIZE * sizeof(int));
      }
  }


  __global__ void test(int *d_sync, int *d_async, int *d_sync_cal, int *d_async_cal)
  {
      int *src = (int *)malloc(SIZE * sizeof(int));
      int *dst = (int *)malloc(SIZE * sizeof(int));
      if (threadIdx == 0) {
          for (int i = 0; i < SIZE; i++) {
               src[i] = i;
          }
      }
      // 阻塞接口测试
      test_sync(d_sync, dst, src, d_sync_cal);
      // 非阻塞接口测试
      test_async(d_async, dst, src, d_async_cal);


      // 打印性能数据
      perf_print();
      free(dst);
      free(src);
  }


  int main()
  {
      const int thread_cnt = 8;
      int h_dst_async[SIZE * thread_cnt] = { 0 };
      int h_dst_sync[SIZE * thread_cnt] = { 0 };
      int h_sync_cal[SIZE_CAL * thread_cnt] = { 0 };
      int h_async_cal[SIZE_CAL * thread_cnt] = { 0 };


      for (int i = 0; i < SIZE_CAL * thread_cnt; i++) {
          h_sync_cal[i] = i;
          h_async_cal[i] = i;
      }


      // 设置使用0号SPA
      sdaaSetDevice(0);
      int *d_dst_async = NULL;
      int *d_dst_sync = NULL;
      int *d_sync_cal = NULL;
      int *d_async_cal = NULL;


      // 在Global内存中malloc一段空间
      sdaaMalloc((void**)&d_dst_async, SIZE * thread_cnt * sizeof(int));
      sdaaMalloc((void**)&d_dst_sync, SIZE * thread_cnt * sizeof(int));
      sdaaMalloc((void**)&d_sync_cal, SIZE_CAL * thread_cnt * sizeof(int));
      sdaaMalloc((void**)&d_async_cal, SIZE_CAL * thread_cnt * sizeof(int));


      // 将数据从Host端拷贝到Device端
      sdaaMemcpy(d_dst_async, h_dst_async, SIZE * thread_cnt * sizeof(int), sdaaMemcpyHostToDevice);
      sdaaMemcpy(d_dst_sync, h_dst_sync, SIZE * thread_cnt * sizeof(int), sdaaMemcpyHostToDevice);
      sdaaMemcpy(d_sync_cal, h_sync_cal, SIZE_CAL * thread_cnt * sizeof(int), sdaaMemcpyHostToDevice);
      sdaaMemcpy(d_async_cal, h_async_cal, SIZE_CAL * thread_cnt * sizeof(int), sdaaMemcpyHostToDevice);


      // kernel函数调用
      test<<<1>>>(d_dst_sync, d_dst_async, d_sync_cal, d_async_cal);


      // 等待kernel函数执行完成
      sdaaDeviceSynchronize();
      // 将数据从Device端拷贝到Host端
      sdaaMemcpy(h_dst_async, d_dst_async, SIZE * thread_cnt * sizeof(int), sdaaMemcpyDeviceToHost);
      sdaaMemcpy(h_dst_sync, d_dst_sync, SIZE * thread_cnt * sizeof(int), sdaaMemcpyDeviceToHost);
      sdaaMemcpy(h_sync_cal, d_sync_cal, SIZE_CAL * thread_cnt * sizeof(int), sdaaMemcpyDeviceToHost);
      sdaaMemcpy(h_async_cal, d_async_cal, SIZE_CAL * thread_cnt * sizeof(int), sdaaMemcpyDeviceToHost);


      for (int i = 0; i < SIZE * thread_cnt; i++) {
          if ((h_dst_sync[i] != i % SIZE) || (h_dst_async[i] != i % SIZE)) {
              printf("h_dst_sync[%d] = %d, h_dst_async[%d] = %d\n", i, h_dst_sync[i], i, h_dst_async[i]);
          }
      }


      for (int i = 0; i < SIZE_CAL * thread_cnt; i++) {
          if ((h_sync_cal[i] != i + TIME) || (h_async_cal[i] != i + TIME)) {
              printf("h_sync_cal[%d] = %d, h_async_cal[%d] = %d\n",
                     i, h_sync_cal[i], i, h_async_cal[i]);
          }
      }


      // 释放申请的Global内存
      sdaaFree(d_sync_cal);
      sdaaFree(d_async_cal);
      sdaaFree(d_dst_async);
      sdaaFree(d_dst_sync);
      return 0;
  }
```

#### 优化效果

| 接口类型 | 数据计算总执行周期（拍） |
|---|---|
| 阻塞型接口 | 2301 |
| 非阻塞型接口 | 1395 |

在本示例中，使用非阻塞型接口，相对于阻塞型接口的数据计算总执行周期减少了 906 拍左右，性能是其约 1.64 倍。

## 4.2.5 原子操作

本文主要介绍原子操作的性能优化策略和示例。

原子操作除 atomic_inc（详情请参考《SDAA C 编程指南》的 atomic_inc 章节）外，返回旧值的接口相比不返回旧值的接口会多一些操作，导致性能上的差别。

#### 优化策略

当不需要返回指定地址上的旧值时，可以使用不返回旧值的原子操作接口提升性能：

- 使用 atomic_add_noret（详情请参考《SDAA C 编程指南》的 atomic_add_noret 章节）代替 atomic_add（详情请参考《SDAA C 编程指南》的 atomic_add 章节）。
- 使用 atomic_sub_noret（详情请参考《SDAA C 编程指南》的 atomic_sub_noret 章节）代替 atomic_sub（详情请参考《SDAA C 编程指南》的 atomic_sub 章节）。
- 使用 atomic_cas_bool（详情请参考《SDAA C 编程指南》的 atomic_cas_bool 章节）代替 atomic_cas（详情请参考《SDAA C 编程指南》的 atomic_cas 章节）。

#### 优化示例

同一 SPA 上的所有 SPE 分别使用有返回值的原子操作接口和没有返回值的原子操作接口，测试并比较以下操作的执行周期：

- 所有 SPE 均执行 1000 次 atomic_add_noret（详情请参考《SDAA C 编程指南》的 atomic_add_noret 章节）和 atomic_add（详情请参考《SDAA C 编程指南》的 atomic_add 章节）。
- 所有 SPE 均执行 1000 次 atomic_sub_noret（详情请参考《SDAA C 编程指南》的 atomic_sub_noret 章节）和 atomic_sub（详情请参考《SDAA C 编程指南》的 atomic_sub 章节）。
- 所有 SPE 均执行 1000 次 atomic_cas_bool（详情请参考《SDAA C 编程指南》的 atomic_cas_bool 章节）和 atomic_cas（详情请参考《SDAA C 编程指南》的 atomic_cas 章节）。

#### 测试环境

| TecoDriver 和 TecoToolKit 版本 | 计算核心频率 |
|---|---|
| v1.2.0 | 2.36GHz |

#### 测试代码

```cpp
  #include <sdaa_perf.h>
  #include <sdaa_atomic.h>
  using namespace sdaa;


  __device__ int global_val;


  // 使用atomic_add接口
  __global__ void test_atomic_add()
  {
          // 数据初始化
          const int step = 10;
          if (threadIdx == 0) global_val = 0;
          // 对每个线程执行1000次atomic_add
          sync_threads();
          perf_start();
          for (int i = 0; i < 1000; i++) {
              atomic_add(&global_val, step);
      }
      perf_stop();


      // 检查结果的正确性
      sync_threads();
      if (threadIdx != 0) return;
      if (global_val != 1000 * threadDim * step) {
          printf("Error: atomic operations not working correctly!\n");
      }
      // 输出性能数据
      perf_print();
  }


  // 使用atomic_add_noret接口
  __global__ void test_atomic_add_noret()
  {
      // 数据初始化
      const int step = 10;
      if (threadIdx == 0) global_val = 0;
      // 对每个线程执行1000次atomic_add_noret
      sync_threads();
      perf_start();
      for (int i = 0; i < 1000; i++) {
          atomic_add_noret(&global_val, step);
      }
      perf_stop();


      // 检查结果的正确性
      sync_threads();
      if (threadIdx != 0) return;
      if (global_val != 1000 * threadDim * step) {
          printf("Error: atomic operations not working correctly!\n");
      }
      // 输出性能数据
      perf_print();
  }


  // 使用atomic_sub接口
  __global__ void test_atomic_sub()
  {
      // 数据初始化
      const int step = 10;
      if (threadIdx == 0) global_val = 0;
      // 对每个线程执行1000次atomic_sub
      sync_threads();
      perf_start();
      for (int i = 0; i < 1000; i++) {
          atomic_sub(&global_val, step);
      }
      perf_stop();


      // 检查结果的正确性
      sync_threads();
      if (threadIdx != 0) return;
      if (global_val != -1000 * threadDim * step) {
          printf("Error: atomic operations not working correctly!\n");
      }
      // 输出性能数据
      perf_print();
  }


  // 使用atomic_sub_noret接口
  __global__ void test_atomic_sub_noret()
  {
      // 数据初始化
      const int step = 10;
      if (threadIdx == 0) global_val = 0;
      // 对每个线程执行1000次atomic_sub_noret
      sync_threads();
      perf_start();
      for (int i = 0; i < 1000; i++) {
          atomic_sub_noret(&global_val, step);
      }
      perf_stop();


      // 检查结果的正确性
      sync_threads();
      if (threadIdx != 0) return;
      if (global_val != -1000 * threadDim * step) {
          printf("Error: atomic operations not working correctly!\n");
      }
      // 输出性能数据
      perf_print();
  }


  // 使用atomic_cas接口
  __global__ void test_atomic_cas()
  {
      // 数据初始化
      const int step = 10;
      if (threadIdx == 0) global_val = 0;
      // 对每个线程执行1000次atomic_cas
      sync_threads();
      perf_start();
      for (int i = 0; i < 1000; i++) {
          atomic_cas(&global_val, step * i, step * (i + 1));
      }
      perf_stop();


      // 检查结果的正确性
      sync_threads();
      if (threadIdx != 0) return;
      if (global_val != 1000 * step) {
          printf("Error: atomic operations not working correctly!\n");
      }
      // 输出性能数据
      perf_print();
  }


  // 使用atomic_cas_bool接口
  __global__ void test_atomic_cas_bool()
  {
      // 数据初始化
      const int step = 10;
      if (threadIdx == 0) global_val = 0;
      // 对每个线程执行1000次atomic_cas_bool
      sync_threads();
      perf_start();
      for (int i = 0; i < 1000; i++) {
          atomic_cas_bool(&global_val, step * i, step * (i + 1));
      }
      perf_stop();


      // 检查结果的正确性
      sync_threads();
      if (threadIdx != 0) return;
      if (global_val != 1000 * step) {
          printf("Error: atomic operations not working correctly!\n");
      }
      // 输出性能数据
      perf_print();
  }


  int main()
  {
      // 设置使用2号SPA
      sdaaSetDevice(2);
      // kernel函数调用
      test_atomic_add<<<1>>>();
      // 等待kernel函数执行完成
      sdaaDeviceSynchronize();
      // kernel函数调用
      test_atomic_add_noret<<<1>>>();
      // 等待kernel函数执行完成
      sdaaDeviceSynchronize();
      // kernel函数调用
      test_atomic_sub<<<1>>>();
      // 等待kernel函数执行完成
      sdaaDeviceSynchronize();
      // kernel函数调用
      test_atomic_sub_noret<<<1>>>();
      // 等待kernel函数执行完成
      sdaaDeviceSynchronize();
      // kernel函数调用
      test_atomic_cas<<<1>>>();
      // 等待kernel函数执行完成
      sdaaDeviceSynchronize();
      // kernel函数调用
      test_atomic_cas_bool<<<1>>>();
      // 等待kernel函数执行完成
      sdaaDeviceSynchronize();
      return 0;
  }
```

#### 优化效果

| 接口名称 | 平均每个 SPE 每次执行周期（拍） |
|---|---|
| atomic_add | 125666 |
| atomic_add_noret | 7193 |
| atomic_sub | 126857 |
| atomic_sub_noret | 7259 |
| atomic_cas | 15423 |
| atomic_cas_bool | 7404 |

在本示例中，测试平均每个 SPE 每次执行原子操作的执行周期：

- atomic_add_noret（详情请参考《SDAA C 编程指南》的 atomic_add_noret 章节）接口的性能是 atomic_add（详情请参考《SDAA C 编程指南》的 atomic_add 章节）接口的约 17.47 倍。
- atomic_sub_noret（详情请参考《SDAA C 编程指南》的 atomic_sub_noret 章节）接口的性能是 atomic_sub（详情请参考《SDAA C 编程指南》的 atomic_sub 章节）接口的约 17.47 倍。
- atomic_cas_bool（详情请参考《SDAA C 编程指南》的 atomic_cas_bool 章节）接口的性能是 atomic_cas（详情请参考《SDAA C 编程指南》的 atomic_cas 章节）接口的约 2.08 倍。

## 4.2.6 矩阵乘

### 4.2.6.1 阻塞型矩阵乘

矩阵乘阻塞型接口相比非阻塞型接口具有更少的使用限制和更高的通用性，但相应地性能也会差于非阻塞型接口。当满足一定对齐条件时，矩阵乘阻塞型接口的性能可以提升到接近非阻塞型接口的性能。

#### 优化策略

当阻塞型矩阵乘接口满足一下条件时，性能较好：

k（输入矩阵列数、权重矩阵行数）和 n（权重矩阵列数）均是 32 的整数倍。

当使用阻塞型矩阵乘接口时，预先将矩阵分块为 32 大小的整数倍，可以有效提高矩阵乘接口的性能。

#### 优化示例

单一线程使用阻塞型矩阵乘接口计算 SPM 上矩阵的乘积时，测试以下两种情况的执行周期并比较：

- k 和 n 均为 32 的整数倍（m=128，k=64，n=32）。
- k 和 n 不是 32 的整数倍（m=128，k=60，n=30）。

#### 测试环境

| TecoDriver 和 TecoToolKit 版本 | 计算核心频率 |
|---|---|
| v1.2.0 | 2.36GHz |

#### 测试代码

```cpp
  #include <sdaa_perf.h>
  #include <sdaa_matmul.h>
  using namespace sdaa;


  __device__ void matmul_perf(half *output, half *input, half *weight,
                              unsigned int m, unsigned int k, unsigned int n)
  {
      // 获取性能数据起始值
      perf_start();
      for (int i = 0; i < 1000; i++) {
          matmul(MatmulHalfToHalf, output, input, weight, m, k, n);
      }
      // 获取性能数据结束值
      perf_stop();
      // 输出性能数据
      perf_print();
  }


  __device__ void init_matrix(half *mat, unsigned int m, unsigned int n)
  {
      // 初始化矩阵数组
      for (unsigned int i = 0; i < m; i++) {
          for (unsigned int j = 0; j < n; j++) {
              mat[i * n + j] = (half) m / (half) n;
          }
      }
  }


  __global__ void test_aligned()
  {
      if (threadIdx != 0) return;
      const unsigned int m = 128, k = 64, n = 32;
      half *input = (half *)malloc(m * k * sizeof(half));
      half *weight = (half *)malloc(k * n * sizeof(half));
      half *output = (half *)malloc(m * n * sizeof(half));
      init_matrix(input, m, k);
      init_matrix(weight, k, n);
      matmul_perf(output, input, weight, m, k, n);
      free(output);
      free(weight);
      free(input);
  }


  __global__ void test_unaligned()
  {
      if (threadIdx != 0) return;
      const unsigned int m = 128, k = 60, n = 30;
      half *input = (half *)malloc(m * k * sizeof(half));
      half *weight = (half *)malloc(k * n * sizeof(half));
      half *output = (half *)malloc(m * n * sizeof(half));
      init_matrix(input, m, k);
      init_matrix(weight, k, n);
      matmul_perf(output, input, weight, m, k, n);
      free(output);
      free(weight);
      free(input);
  }


  int main()
  {
      // 设置使用0号SPA
      sdaaSetDevice(2);
      // kernel函数调用
      test_aligned<<<1>>>();
      // 等待kernel函数执行完成
      sdaaDeviceSynchronize();
      // kernel函数调用
      test_unaligned<<<1>>>();
      // 等待kernel函数执行完成
      sdaaDeviceSynchronize();
      return 0;
  }
```

#### 优化效果

| 矩阵维度大小 | 平均每次阻塞型矩阵乘接口的执行周期（拍） |
|---|---|
| m=128，k=64，n=32 | 26993 |
| m=128，k=60，n=30 | 146818 |

在本示例中，k 和 n 非 32 整数倍（k=60，n=30）的矩阵乘数据量更小，但执行周期更长。k 和 n 是 32 整数倍（k=64，n=32）的矩阵乘虽然多了一些数据，但性能却是非 32 整数倍的矩阵乘的 5.43 倍。出现这个性能差距的原因是非 32 整数倍的矩阵需要重新切分组装数据，耗时较多。

### 4.2.6.2 非阻塞型矩阵乘

本文主要介绍非阻塞型矩阵乘的性能优化策略和示例。

#### 优化策略

如果搬运的数据与当前程序的后续操作不存在数据依赖时，使用非阻塞式接口（详情请参考《SDAA C 编程指南》的非阻塞型矩阵乘章节）可以更好的进行流水排布，相对阻塞式接口（详情请参考《SDAA C 编程指南》的阻塞型矩阵乘章节）性能更好。

#### 优化示例

在设备端测试以下情况的数据计算总执行周期并比较：

- 使用阻塞型矩阵乘接口：先执行矩阵乘，再执行与矩阵乘无数据依赖的计算操作。
- 使用非阻塞型矩阵乘接口：同步执行矩阵乘和与矩阵乘无数据依赖的计算操作。

每个测试均调用 10000 次矩阵乘操作和与矩阵乘无数据依赖的计算操作，从而计算平均值以消除初次调用时 cache 影响。

#### 测试环境

| TecoDriver 和 TecoToolKit 版本 | 计算核心频率 | 输入矩阵大小 | 权重矩阵大小 |
|---|---|---|---|
| v1.2.0 | 2.36GHz | 128 × 32 | 32 × 32 |

#### 测试代码

```cpp
  #include <sdaa_matmul.h>
  #include <sdaa_perf.h>
  using namespace sdaa;


  #define WEIGHT_ROW 32
  #define WEIGHT_COL 32
  #define INTPUT_ROW 128
  #define INTPUT_COL 32
  #define OUTPUT_ROW 128
  #define OUTPUT_COL 32
  #define SIZE_CAL 256
  #define TIME 10000


  #define INTPUT_SIZE (INTPUT_ROW * INTPUT_COL * sizeof(short))
  #define WEIGHT_SIZE (WEIGHT_ROW * WEIGHT_COL * sizeof(short))
  #define OUTPUT_SIZE (OUTPUT_ROW * OUTPUT_COL * sizeof(short))


  __device__ void test_sync(short *input, short *weight, short *output, int *cal)
  {
          short *ibuffer = (short *)malloc(INTPUT_SIZE);
          short *wbuffer = (short *)malloc(WEIGHT_SIZE);
          short *obuffer = (short *)malloc(OUTPUT_SIZE);


          memcpy(ibuffer, input, INTPUT_SIZE);
          memcpy(wbuffer, weight, WEIGHT_SIZE);
          memcpy(obuffer, output, OUTPUT_SIZE);


          intv16 cal_src;
          intv16 add = 1;
          // 统计性能数据
          perf_start();


          for (int time = 0; time < TIME; time++) {
              matmul(MatmulShortToShort, obuffer, ibuffer, wbuffer, INTPUT_ROW, WEIGHT_ROW, OUTPUT_COL);
              // 异步进行与数据搬运无数据依赖的计算
              for (int idx = 0; idx < SIZE_CAL; idx += 16) {
                  // 使用向量进行计算
                  simd_load(cal_src, cal + idx);
                  intv16 tmp = cal_src + add;
                  simd_store(tmp, cal + idx);
              }
      }
      // 统计性能数据
      perf_stop();


      memcpy(output, obuffer, OUTPUT_SIZE);


      free(ibuffer);
      free(wbuffer);
      free(obuffer);
  }


  __device__ void test_async(short *input, short *weight, short *output, int *cal)
  {
      short *ibuffer = (short *)malloc(INTPUT_SIZE);
      short *wbuffer = (short *)malloc(WEIGHT_SIZE);
      short *obuffer = (short *)malloc(OUTPUT_SIZE);


      memcpy(ibuffer, input, INTPUT_SIZE);
      memcpy(wbuffer, weight, WEIGHT_SIZE);
      memcpy(obuffer, output, OUTPUT_SIZE);


      MatmulHandle handle;
      intv16 cal_src;
      intv16 add = 1;
      // 统计性能数据
      perf_start();


      for (int time = 0; time < TIME; time++) {
          // 初始化handle
          matmul_init(handle, MatmulShortToShort);
          // 加载权重矩阵
          matmul_load_weight(handle, wbuffer, MatmulK32, MatmulN32);
          // 加载输入矩阵并与权重矩阵进行矩阵乘运算
          matmul_compute(handle, ibuffer, INTPUT_ROW, MatmulK32);
          // 将矩阵乘运算结果写回到指定地址
          matmul_store(handle, obuffer, OUTPUT_ROW, MatmulN32);


          for (int idx = 0; idx < SIZE_CAL; idx += 16) {
              // 使用向量进行计算
              simd_load(cal_src, cal + idx);
              intv16 tmp = cal_src + add;
              simd_store(tmp, cal + idx);
          }
          // 等待矩阵乘写回操作完成
          matmul_wait(handle);
      }
      // 统计性能数据
      perf_stop();


      memcpy(output, obuffer, OUTPUT_SIZE);


      free(ibuffer);
      free(wbuffer);
      free(obuffer);
  }


  __global__ void test(short *input, short *weight, short *output_sync,
                        short *output_async, int *cal_sync, int *cal_async)
  {
      if (threadIdx != 0) {
          return;
      }


      // 测试同步接口
      test_sync(input, weight, output_sync, cal_sync);
      // 测试异步接口
      test_async(input, weight, output_async, cal_async);


      perf_print();
  }


  // 初始化数组
  void SetArrayVal(short *array, int num, short val) {
      for (int i = 0; i < num; i++) {
          array[i] = val;
      }
  }


  void entry(short *h_intput, short *h_weight, short *h_output_sync,
              short *h_output_async, int *h_cal_sync, int *h_cal_async)
  {
      // 设置使用0号SPA
      sdaaSetDevice(0);
      short *dev_intput = NULL;
      short *dev_weight = NULL;
      short *dev_output_sync = NULL;
      short *dev_output_async = NULL;
      int *dev_cal_sync = NULL;
      int *dev_cal_async = NULL;


      // 在Global内存中malloc一段空间
      sdaaMalloc((void **)(&dev_intput), INTPUT_SIZE);
      sdaaMalloc((void **)(&dev_weight), WEIGHT_SIZE);
      sdaaMalloc((void **)(&dev_output_sync), OUTPUT_SIZE);
      sdaaMalloc((void **)(&dev_output_async), OUTPUT_SIZE);
      sdaaMalloc((void **)(&dev_cal_sync), SIZE_CAL * sizeof(int));
      sdaaMalloc((void **)(&dev_cal_async), SIZE_CAL * sizeof(int));


      // 将数据从Host端拷贝到Device端
      sdaaMemcpy(dev_intput, h_intput, INTPUT_SIZE, sdaaMemcpyHostToDevice);
      sdaaMemcpy(dev_weight, h_weight, WEIGHT_SIZE, sdaaMemcpyHostToDevice);
      sdaaMemcpy(dev_output_sync, h_output_sync, OUTPUT_SIZE, sdaaMemcpyHostToDevice);
      sdaaMemcpy(dev_output_async, h_output_async, OUTPUT_SIZE, sdaaMemcpyHostToDevice);
      sdaaMemcpy(dev_cal_sync, h_cal_sync, SIZE_CAL * sizeof(int), sdaaMemcpyHostToDevice);
      sdaaMemcpy(dev_cal_async, h_cal_async, SIZE_CAL * sizeof(int), sdaaMemcpyHostToDevice);


      // kernel函数调用
      test<<<1>>>(dev_intput, dev_weight, dev_output_sync, dev_output_async,
                    dev_cal_sync,   dev_cal_async);
      // 等待kernel函数执行完成
      sdaaDeviceSynchronize();


      // 将数据从Device端拷贝到Host端
      sdaaMemcpy(h_output_sync, dev_output_sync, OUTPUT_SIZE, sdaaMemcpyDeviceToHost);
      sdaaMemcpy(h_output_async, dev_output_async, OUTPUT_SIZE, sdaaMemcpyDeviceToHost);
      sdaaMemcpy(h_cal_sync, dev_cal_sync, SIZE_CAL * sizeof(int), sdaaMemcpyDeviceToHost);
      sdaaMemcpy(h_cal_async, dev_cal_async, SIZE_CAL * sizeof(int), sdaaMemcpyDeviceToHost);


      // 释放申请的Global内存
      sdaaFree(dev_intput);
      sdaaFree(dev_weight);
      sdaaFree(dev_output_sync);
      sdaaFree(dev_output_async);
      sdaaFree(dev_cal_sync);
      sdaaFree(dev_cal_async);
  }


  int main()
  {
      short *h_intput = (short *)malloc(INTPUT_SIZE);
      short *h_weight = (short *)malloc(WEIGHT_SIZE);
      short *h_output_sync = (short *)malloc(OUTPUT_SIZE);
      short *h_output_async = (short *)malloc(OUTPUT_SIZE);


      int *h_cal_sync = (int *)malloc(SIZE_CAL * sizeof(int));
      int *h_cal_async = (int *)malloc(SIZE_CAL * sizeof(int));


      SetArrayVal(h_intput, INTPUT_ROW * INTPUT_COL, 1);
      SetArrayVal(h_weight, WEIGHT_ROW * WEIGHT_COL, 2);
      memset(h_output_sync, 0, OUTPUT_SIZE);
      memset(h_output_async, 0, OUTPUT_SIZE);
      memset(h_cal_sync, 0, SIZE_CAL * sizeof(int));
      memset(h_cal_async, 0, SIZE_CAL * sizeof(int));
      const short REFEROUT = 64;


      entry(h_intput, h_weight, h_output_sync, h_output_async, h_cal_sync, h_cal_async);


      for (int row = 0; row < OUTPUT_ROW; row++) {
          for (int col = 0; col < OUTPUT_COL; col++) {
               if ((h_output_sync[row * OUTPUT_COL + col] != REFEROUT) ||
                   (h_output_async[row * OUTPUT_COL + col] != REFEROUT)) {
                   printf("h_output_sync[%d] = %d, h_output_async[%d] = %d\n",
                           row * OUTPUT_COL + col, h_output_sync[row * OUTPUT_COL + col],
                           row * OUTPUT_COL + col, h_output_async[row * OUTPUT_COL + col]);
               }
          }
      }


      for (int i = 0; i < SIZE_CAL; i++) {
          if ((h_cal_sync[i] != TIME) || (h_cal_async[i] != TIME)) {
               printf("h_cal_sync[%d] = %d, h_cal_async[%d] = %d\n",
                      i, h_cal_sync[i], i, h_cal_async[i]);
          }
      }


      free(h_intput);
      free(h_weight);
      free(h_output_sync);
      free(h_output_async);
      free(h_cal_sync);
      free(h_cal_async);
      return 0;
  }
```

#### 优化效果

| 矩阵乘类型 | 计算总执行周期（拍） |
|---|---|
| 阻塞型矩阵乘 | 3347 |
| 非阻塞型矩阵乘 | 1453 |

在本示例中，使用非阻塞型矩阵乘，相对于使用阻塞型矩阵乘的计算总执行周期减少了 1894 拍左右，性能是其约 2.30 倍。

## 4.2.7 向量操作

### 4.2.7.1 性能数据

本文主要介绍不同向量接口的性能数据。

> **说明：**
> 本文的向量接口性能数值会有 10% 左右的波动，具体请以实际情况为准。

使用 SIMD 接口，源地址和目的地址的数据均位于 SPM 存储空间，各向量接口的性能如下：

| 接口类别 | 接口名称 | 接口性能（执行周期，单位：拍） |
|---|---|---|
| 装入/存储类操作 | simd_load | 12 |
| 装入/存储类操作 | simd_loadu | 16 |
| 装入/存储类操作 | simd_store | 7 |
| 装入/存储类操作 | simd_storeu | 18 |
| 装入/存储类操作 | simd_load_widen | 12 |
| 装入/存储类操作 | simd_store_narrow | 7 |
| 标量扩展类操作 | simd_set | 3 |
| 标量扩展类操作 | simd_stretch | 10 |
| 比较选择类操作 | simd_seleq | 15 |
| 比较选择类操作 | simd_selle | 15 |
| 比较选择类操作 | simd_sellt | 15 |
| 元素整理类操作 | simd_concat | 10 |
| 元素整理类操作 | simd_ins | 7 |
| 符号拷贝类操作 | simd_copy_sign | 10 |
| 类型转换类操作 | simd_cvt_f2h | 5 |
| 类型转换类操作 | simd_cvt_h2f | 5 |
| 融合乘加类操作 | simd_fma | 15 |
| 融合乘加类操作 | simd_fms | 15 |
| 融合乘加类操作 | simd_fnma | 15 |
| 融合乘加类操作 | simd_fnms | 15 |
| 算数运算类操作 | simd_add | 12 |
| 算数运算类操作 | simd_sub | 12 |
| 算数运算类操作 | simd_mul | 12 |
| 算数运算类操作 | simd_div | 1723 |
| 算数运算类操作 | simd_mod | 1670 |
| 算数运算类操作 | simd_fabs | 3 |
| 算数运算类操作 | simd_redsum | 3 |
| 比较类操作 | simd_cmplt | 12 |
| 比较类操作 | simd_cmple | 12 |
| 比较类操作 | simd_cmpgt | 12 |
| 比较类操作 | simd_cmpge | 12 |
| 比较类操作 | simd_cmpeq | 12 |
| 比较类操作 | simd_cmpne | 15 |
| 比较类操作 | simd_isnan | 21 |
| 比较类操作 | simd_max | 22 |
| 比较类操作 | simd_min | 22 |
| 逻辑运算类操作 | simd_and | 100 |
| 逻辑运算类操作 | simd_or | 16 |
| 逻辑运算类操作 | simd_not | 11 |
| 逻辑运算类操作 | simd_neg | 11 |
| 按位运算类操作 | simd_band | 10 |
| 按位运算类操作 | simd_bxor | 10 |
| 按位运算类操作 | simd_bor | 10 |
| 按位运算类操作 | simd_bnor | 11 |
| 按位运算类操作 | simd_bnot | 10 |

### 4.2.7.2 优化策略

本文主要介绍向量操作的性能优化策略和示例。

#### 优化策略

在使用 SIMD 接口时，源地址和目的地址的数据均位于 SPM 存储空间，性能更好。

#### 优化示例

使用 simd_load（详情请参考《SDAA C 编程指南》的 simd_load 章节）接口，测试并比较以下两种情况，一个 SPE 执行 1000 次的执行周期：

- 源地址和目的地址的数据均位于 SPM 存储空间。
- 源地址和目的地址的数据均位于 Global 存储空间。

#### 测试环境

| TecoDriver 和 TecoToolKit 版本 | 计算核心频率 |
|---|---|
| v1.2.0 | 2.36GHz |

#### 测试代码

```cpp
  #include <sdaa_perf.h>
  using namespace sdaa;
  // 在Global内存空间定义变量
  volatile __device__ intv16 glb_vdata;


  __device__ void test_simd_load_in_global(int *glb_data)
  {
          // 收集性能数据起始值
          perf_start();
          for (int i = 0; i < 1000; i++){
              simd_load(glb_vdata, glb_data);
          }
          // 收集性能数据结束值
          perf_stop();
  }


  __device__ void test_simd_load_in_spm(int *glb_data)
  {
          int *spm_data = (int *)malloc(16 * sizeof(int));
          memcpy(spm_data, glb_data, 16 * sizeof(int));
          volatile intv16 spm_vdata;


          perf_start();
          for (int i = 0; i < 1000; i++) {
              simd_load(spm_vdata, spm_data);
          }
          perf_stop();


          free(spm_data);
  }


  __global__ void test_func(int *glb_data)
  {
          if (threadIdx != 0) return;


          test_simd_load_in_global(glb_data);
          test_simd_load_in_spm(glb_data);
          // 输出性能数据
          perf_print();
  }


  int main()
  {
      // 设置使用0号SPA
      sdaaSetDevice(0);
      int h_glb_data[16] = { 0 };


      for (int i = 0; i < 16; i++){
          h_glb_data[i] = i;
      }


      int *glb_data = NULL;
      // 在Global内存中malloc一段空间
      sdaaMalloc((void **)&glb_data, 16 * sizeof(int));
      // 将数据从Host端拷贝到Device端
      sdaaMemcpy(glb_data, h_glb_data, 16 * sizeof(int), sdaaMemcpyHostToDevice);


      // kernel函数调用
      test_func<<<1>>>(glb_data);
      // 等待kernel函数执行完成
      sdaaDeviceSynchronize();


      // 释放申请的Global内存
      sdaaFree(glb_data);
      return 0;
  }
```

#### 优化效果

| 接口名称 | 接口性能（执行周期，单位：拍）（源地址和目的地址的数据均位于 Global 存储空间） | 接口性能（执行周期，单位：拍）（源地址和目的地址的数据均位于 SPM 存储空间） |
|---|---|---|
| simd_load | 758 | 12 |

在本示例中，源地址和目的地址的数据均位于 SPM 存储空间，比源地址和目的地址的数据均位于 Global 存储空间，向量接口性能快约 63 倍。

## 4.3 数学函数

### 4.3.1 精度和性能数据

本文主要介绍不同数学函数接口的精度和性能。

> **说明：**
> 本文的接口性能数值会有 10% 左右的波动，具体请以实际情况为准。

#### 基础函数

| 类别 | 精度类型 | 函数名 | 精度 | 接口性能（执行周期，单位：拍） |
|---|---|---|---|---|
| 指数函数 | 双精度 | exp(x) | [-103.97, 88.72] 内最大 ULP 为 150 | 151 |
| 指数函数 | 单精度 | expf(x) | 最大 ULP 为 1 | 158 |
| 指数函数 | 向量 | simd_exp(x) | 最大 ULP 为 5 | 205 |
| 指数函数 | 128 | simd_exp128(x) | [-19.4, 88.72] 内最大 ULP 为 197；否则最大绝对误差为 4e-9 | 满足 128 个输入全部小于 2.77 时：523；否则：1214 |
| 指数减一的函数 | 双精度 | expm1(x) | 最大 ULP 为 1 | 330 |
| 指数减一的函数 | 单精度 | expm1f(x) | 最大 ULP 为 1 | 352 |
| 对数函数（以 e 为底） | 双精度 | log(x) | 最大 ULP 为 4 | 218 |
| 对数函数（以 e 为底） | 单精度 | logf(x) | 最大 ULP 为 4 | 188 |
| 对数函数（以 e 为底） | 向量 | simd_log(x) | 最大 ULP 为 5 | 218 |
| 对数函数（以 e 为底） | 128 | simd_log128(x) | 最大 ULP 为 5 | 1145 |
| 加一的对数函数（以 e 为底） | 双精度 | log1p(x) | 最大 ULP 为 1 | 334 |
| 加一的对数函数（以 e 为底） | 单精度 | log1pf(x) | 最大 ULP 为 1 | 356 |
| 对数函数（以 2 为底） | 双精度 | log2(x) | 最大 ULP 为 6 | 217 |
| 对数函数（以 2 为底） | 单精度 | log2f(x) | 最大 ULP 为 5 | 166 |
| 对数函数（以 2 为底） | 向量 | simd_log2(x) | 最大 ULP 为 6 | 197 |
| 对数函数（以 2 为底） | 128 | simd_log2_128(x) | 最大 ULP 为 6 | 1146 |
| 平方根函数 | 双精度 | sqrt(x) | 最大 ULP 为 0 | 45 |
| 平方根函数 | 单精度 | sqrtf(x) | 最大 ULP 为 0 | 31 |
| 平方根函数 | 向量 | simd_sqrt(x) | 最大 ULP 为 2 | 98 |
| 倒数函数 | 双精度 | 1/x | 最大 ULP 为 0 | 47 |
| 倒数函数 | 单精度 | 1/x | 最大 ULP 为 0 | 32 |
| 倒数函数 | 向量 | simd_vinv(x) | 最大 ULP 为 1 | 69 |
| 倒数函数 | 128 | simd_vinv128(x) | 最大 ULP 为 1 | 176 |
| 判断是否非数函数 | 双精度 | isnan(x) | 最大 ULP 为 0 | 351 |
| 判断是否非数函数 | 单精度 | isnan(x) | 最大 ULP 为 0 | 351 |
| 绝对值函数 | 双精度 | fabs(x) | 最大 ULP 为 0 | 14 |
| 绝对值函数 | 单精度 | fabsf(x) | 最大 ULP 为 0 | 14 |
| 幂函数 | 双精度 | pow(x) | 最大 ULP 为 1 | 1579 |
| 幂函数 | 单精度 | powf(x) | 最大 ULP 为 3 | 1602 |

#### 激活函数和误差函数

| 类别 | 精度类型 | 函数名 | 精度 | 接口性能（执行周期，单位：拍） |
|---|---|---|---|---|
| 双曲正切函数 | 双精度 | tanh(x) | 最大 ULP 为 3 | 761 |
| 双曲正切函数 | 单精度 | tanhf(x) | 最大 ULP 为 2 | 251 |
| 双曲正切函数 | 向量 | simd_tanh(x) | 最大 ULP 为 32 | 251 |
| 双曲正切函数 | 128 | simd_tanh128(x) | 最大 ULP 为 32 | 635 |
| 高斯误差函数 | 双精度 | erf(x) | 最大 ULP 为 1 | [1.25, 5.999) 内：1079，否则：184 |
| 高斯误差函数 | 单精度 | erff(x) | 最大 ULP 为 1 | [1.25, 5.999) 内：1103，否则：206 |
| 高斯误差函数 | 向量 | simd_erf(x) | 最大 ULP 为 17 | 253 |
| 高斯误差函数 | 128 | simd_erf128(x) | 最大 ULP 为 17 | 1083 |
| sigmoid 函数 | 双精度 | sigmoid(x) | [-10,10] 内最大 ULP 为 124，否则最大绝对误差为 5e-5 | 219 |
| sigmoid 函数 | 单精度 | sigmoidf(x) | [-10,10] 内最大 ULP 为 3，否则最大绝对误差为 5e-5 | 205 |
| sigmoid 函数 | 向量 | simd_sigmoid(x) | [-10,10] 内最大 ULP 为 8，否则最大绝对误差为 1e-6 | 204 |
| sigmoid 函数 | 128 | simd_sigmoid128(x) | [-10,10] 内最大 ULP 为 8，否则最大绝对误差为 1e-6 | 506 |

#### 三角函数、反三角函数

| 类别 | 精度类型 | 函数名 | 精度 | 接口性能（执行周期，单位：拍） |
|---|---|---|---|---|
| 正弦函数 | 双精度 | sin(x) | 最大 ULP 为 1 | 请以实际情况为准 |
| 正弦函数 | 单精度 | sinf(x) | 最大 ULP 为 1 | 请以实际情况为准 |
| 正弦函数 | 向量 | simd_sin(x) | 最大 ULP 为 2 | 140 |
| 正弦函数 | 128 | simd_sin128(x) | 最大 ULP 为 2 | 473 |
| 余弦函数 | 双精度 | cos(x) | 最大 ULP 为 1 | 请以实际情况为准 |
| 余弦函数 | 单精度 | cosf(x) | 最大 ULP 为 1 | 请以实际情况为准 |
| 余弦函数 | 向量 | simd_cos(x) | [-2000,2000] 内最大 ULP 为 2，否则最大绝对误差为 1.28e-7 | 278 |
| 余弦函数 | 128 | simd_cos128(x) | [-2000,2000] 内最大 ULP 为 2，否则最大绝对误差为 1.28e-7 | 1774 |
| 正切函数 | 双精度 | tan(x) | 最大 ULP 为 1 | 请以实际情况为准 |
| 正切函数 | 单精度 | tanf(x) | 最大 ULP 为 1 | 请以实际情况为准 |
| 反正切函数 | 双精度 | atan(x) | 最大 ULP 为 2 | 208 |
| 反正切函数 | 单精度 | atanf(x) | 最大 ULP 为 2 | 214 |
| 反正切函数 | 向量 | simd_atan(x) | 最大 ULP 为 5 | 148 |
| 反正切函数 | 128 | simd_atan128(x) | 最大 ULP 为 5 | 523 |

#### 浮点取整函数

| 类别 | 精度类型 | 函数名 | 精度 | 接口性能（执行周期，单位：拍） |
|---|---|---|---|---|
| 向上取整函数 | 双精度 | ceil(x) | 最大 ULP 为 0 | 77 |
| 向上取整函数 | 单精度 | ceilf(x) | 最大 ULP 为 0 | 99 |
| 向下取整函数 | 双精度 | floor(x) | 最大 ULP 为 0 | 73 |
| 向下取整函数 | 单精度 | floorf(x) | 最大 ULP 为 0 | 95 |
| 向下取整函数 | 向量 | simd_floor(x) | 最大 ULP 为 0 | 57 |
| 向下取整函数 | 128 | simd_floor128(x) | 最大 ULP 为 0 | 205 |
| 就近取整函数 | 双精度 | round(x) | 最大 ULP 为 0 | 72 |
| 就近取整函数 | 单精度 | roundf(x) | 最大 ULP 为 0 | 99 |
| 就近取整函数 | 向量 | simd_round(x) | 最大 ULP 为 0 | 53 |
| 就近取整函数 | 128 | simd_round128(x) | 最大 ULP 为 0 | 131 |
| 向零取整函数 | 向量 | simd_trunc(x) | 最大 ULP 为 0 | 47 |
| 向零取整函数 | 128 | simd_trunc128(x) | 最大 ULP 为 0 | 121 |

#### 二元函数

| 类别 | 精度类型 | 函数名 | 精度 | 接口性能（执行周期，单位：拍） |
|---|---|---|---|---|
| 浮点乘加函数 | 双精度 | fma(x) | 最大 ULP 为 0 | 54 |
| 浮点乘加函数 | 单精度 | fmaf(x) | 最大 ULP 为 0 | 39 |
| 最大值函数 | 双精度 | fmax(x) | 最大 ULP 为 0 | 101 |
| 最大值函数 | 单精度 | fmaxf(x) | 最大 ULP 为 0 | 124 |
| 最小值函数 | 双精度 | fmin(x) | 最大 ULP 为 0 | 101 |
| 最小值函数 | 单精度 | fminf(x) | 最大 ULP 为 0 | 129 |

### 4.3.2 优化策略

SDAA C 提供的数学函数接口，除标量（单精度（详情请参考《SDAA C 编程指南》的单精度标量接口章节）、双精度（详情请参考《SDAA C 编程指南》的双精度标量接口章节））外还提供了向量接口和 128 接口（详情请参考《SDAA C 编程指南》的向量接口章节）：

| 接口类别 | 输入类型 | 输出类型 | 说明 |
|---|---|---|---|
| 单精度标量接口 | 单精度浮点数 | 单精度浮点数 | 与标准库一致，计算一个单精度浮点数的数学函数值。 |
| 双精度标量接口 | 双精度浮点数 | 双精度浮点数 | 与标准库一致，计算一个双精度浮点数的数学函数值。 |
| 向量接口 | floatv16 向量类型 | floatv16 向量类型 | 利用 SIMD 指令，一次计算 16 个单精度浮点数的数学函数值。 |
| 128 接口 | float 型指针 | 无 | 利用 SIMD 指令和循环展开，一次计算 128 个单精度浮点数的数学函数值，并返回覆盖原地址上数据。 |

#### 优化策略

需要计算大量连续的单精度浮点数时，采用向量接口或者 128 接口相比使用单精度标量接口计算每个元素，性能更好。

#### 优化示例

以指数函数 exp 为例，使用一个 SPE 对 51200 个单精度浮点数进行计算，测试并比较以下三种接口的执行周期：

- 单精度标量接口 expf（详情请参考《SDAA C 编程指南》的 expf 章节）。
- 向量接口 simd_exp（详情请参考《SDAA C 编程指南》的 simd_exp 章节）。
- 128 接口 simd_exp128（详情请参考《SDAA C 编程指南》的 simd_exp128 章节）。

#### 测试环境

| TecoDriver 和 TecoToolKit 版本 | 计算核心频率 |
|---|---|
| v1.2.0 | 2.36GHz |

#### 测试代码

```cpp
  #include <sdaa_perf.h>
  using namespace sdaa;


  #define NUM 51200
  #define MIN -80
  #define MAX 80


  __device__ void init_arr(float *arr)
  {
      for (int i = 0; i < NUM; i++) {
          arr[i] = ((float)(MAX - MIN) / (float)NUM) * i + MIN;
      }
  }


  __global__ void test_expf()
  {
      if (threadIdx != 0) return;
      // 在SPM内存中申请一段空间
      float *arr = (float *)malloc(NUM * sizeof(float));
      init_arr(arr);
      // 收集性能数据起始值
      perf_start();
      for (int i = 0; i < NUM; i++) {
          arr[i] = expf(arr[i]);
      }
      // 收集性能数据结束值
      perf_stop();
      // 输出性能数据
      perf_print();
      // 释放申请的SPM内存空间
      free(arr);
  }


  __global__ void test_simd_exp()
  {
      if (threadIdx != 0) return;
      // 在SPM内存中申请一段空间
      float *arr = (float *)malloc(NUM * sizeof(float));
      init_arr(arr);
      // 收集性能数据起始值
      perf_start();
      for (int i = 0; i < NUM; i += 16) {
          floatv16 input;
          simd_load(input, arr + i);
          input = simd_exp(input);
          simd_store(input, arr + i);
      }
      // 收集性能数据结束值
      perf_stop();
      // 输出性能数据
      perf_print();
      // 释放申请的SPM内存空间
      free(arr);
  }


  __global__ void test_simd_exp128()
  {
      if (threadIdx != 0) return;
      // 在SPM内存中申请一段空间
      float *arr = (float *)malloc(NUM * sizeof(float));
      init_arr(arr);
      // 收集性能数据起始值
      perf_start();
      for (int i = 0; i < NUM; i += 128) {
          simd_exp128(arr + i);
      }
      // 收集性能数据结束值
      perf_stop();
      // 输出性能数据
      perf_print();
      // 释放申请的SPM内存空间
      free(arr);
  }


  int main()
  {
      // 设置使用2号SPA
      sdaaSetDevice(2);
      // kernel函数调用
      test_expf<<<1>>>();
      // 等待kernel函数执行完成
      sdaaDeviceSynchronize();
      // kernel函数调用
      test_simd_exp<<<1>>>();
      // 等待kernel函数执行完成
      sdaaDeviceSynchronize();
      // kernel函数调用
      test_simd_exp128<<<1>>>();
      // 等待kernel函数执行完成
      sdaaDeviceSynchronize();
      return 0;
  }
```

#### 优化效果

| 接口名称 | 51200 次数学函数计算的指令周期（拍） | 平均每次数学函数计算的指令周期（拍） |
|---|---|---|
| 单精度标量接口 expf | 7885552 | 154.02 |
| 向量接口 simd_exp | 650860 | 12.71 |
| 128 接口 simd_exp128 | 354080 | 6.92 |

在本示例中，使用向量接口的性能是标量接口的约 12.11 倍，使用 128 接口的性能是标量接口的约 22.26 倍。

## 4.4 程序编译

### 4.4.1 循环展开

本文主要介绍使用循环展开的方式提高性能的优化策略和示例。

#### 原理介绍

循环展开是在编译程序中，通过减少循环迭代次数进而提高程序执行速度的方法。

其核心思想是将操作对象线性化，在一次迭代中访问线性数据中的一组而非单个，使程序在执行更少的迭代次数后，能够有效降地低循环的开销。同时，循环展开可以减少指令的相关性，增加循环体内语句并发执行的可能性，有助于进行更好的指令调度；使用循环展开还可以减少分支预测失败的可能性。但循环展开也会使代码膨胀；代码的可读性降低，以及寄存器压力增大。

循环展开可以在程序编写时完成，也可以由编译器自动完成。由于编译器可以更好地了解目标体系结构的工作原理，并能够在有益的情况下展开循环。因此，建议您使用编译器自动展开，如果您手动展开，建议您在测试手动循环展开确实有效再进行。

#### 优化策略

通过以下编译指示，控制是否需启用循环展开功能，以及展开的次数：

- `#pragma clang loop unroll(enable)` ：启用基于自动诊断的展开功能。
- `#pragma clang loop unroll(disable)` ：禁用基于自动诊断的展开功能。
- `#pragma clang loop unroll(full)` ：完全展开一个循环。
- `#pragma clang loop unroll_count(N)` ：显式指定展开 N 次，其中 N 是编译时的常量。

#### 优化示例

使用不同的编译指示，在 for 循环内对 1000 个整型数进行"+1"的操作，测试以下情况的执行周期并比较：

- 不做任何操作
- `#pragma clang loop unroll(enable)` ：启用基于自动诊断的展开功能。
- `#pragma clang loop unroll(disable)` ：禁用基于自动诊断的展开功能。
- `#pragma clang loop unroll(full)` ：完全展开一个循环。
- `#pragma clang loop unroll_count(1)` ：显式指定展开 1 次。
- `#pragma clang loop unroll_count(2)` ：显式指定展开 2 次。
- `#pragma clang loop unroll_count(3)` ：显式指定展开 3 次。
- `#pragma clang loop unroll_count(4)` ：显式指定展开 4 次。
- `#pragma clang loop unroll_count(5)` ：显式指定展开 5 次。
- `#pragma clang loop unroll_count(6)` ：显式指定展开 6 次。
- `#pragma clang loop unroll_count(7)` ：显式指定展开 7 次。
- `#pragma clang loop unroll_count(8)` ：显式指定展开 8 次。
- `#pragma clang loop unroll_count(9)` ：显式指定展开 9 次。
- `#pragma clang loop unroll_count(10)` ：显式指定展开 10 次。

每个测例均调用 10000 次"+1"的操作计算平均值以消除初次调用时 cache 影响。

#### 测试环境

| TecoDriver 和 TecoToolKit 版本 | 计算核心频率 |
|---|---|
| v1.2.0 | 2.36GHz |

#### 测试代码

```cpp
  #include <sdaa_perf.h>
  using namespace sdaa;


  #define N 10000
  #define SIZE 1000


  __device__ void init_data(int *data)
  {
          for (int i = 0; i < SIZE; i++) {
              data[i] = i;
          }
  }


  __device__ void check_result(int *data)
  {
          for (int i = 0; i < SIZE; i++) {
              if (data[i] != i + N) {
                    printf("calculate error\n");
              }
          }
  }


  __device__ void loop_unroll_do_nothing()
  {
          int data[SIZE];
          init_data(data);


          // 收集性能数据起始值
          perf_start();
          for (int i = 0; i < N; i++) {
              for (int j = 0; j < SIZE; j++) {
                    data[j] = data[j] + 1;
              }
          }
          // 收集性能数据结束值
          perf_stop();


          check_result(data);
          return;
  }


  __device__ void loop_unroll_enable()
  {
      int data[SIZE];
      init_data(data);


      // 收集性能数据起始值
      perf_start();
      for (int i = 0; i < N; i++) {
          #pragma clang loop unroll(enable)
          for (int j = 0; j < SIZE; j++) {
                data[j] = data[j] + 1;
          }
      }
      // 收集性能数据结束值
      perf_stop();


      check_result(data);
      return;
  }


  __device__ void loop_unroll_disable()
  {
      int data[SIZE];
      init_data(data);


      // 收集性能数据起始值
      perf_start();
      for (int i = 0; i < N; i++) {
          #pragma clang loop unroll(disable)
          for (int j = 0; j < SIZE; j++) {
                data[j] = data[j] + 1;
          }
      }
      // 收集性能数据结束值
      perf_stop();


      check_result(data);
      return;
  }


  __device__ void loop_unroll_full()
  {
      int data[SIZE];
      init_data(data);


      // 收集性能数据起始值
      perf_start();
      for (int i = 0; i < N; i++) {
          #pragma clang loop unroll(full)
          for (int j = 0; j < SIZE; j++) {
                data[j] = data[j] + 1;
          }
      }
      // 收集性能数据结束值
      perf_stop();


      check_result(data);
      return;
  }


  __device__ void loop_unroll_count_1()
  {
      int data[SIZE];
      init_data(data);


      // 收集性能数据起始值
      perf_start();
      for (int i = 0; i < N; i++) {
          #pragma clang loop unroll_count(1)
          for (int j = 0; j < SIZE; j++) {
                data[j] = data[j] + 1;
          }
      }
      // 收集性能数据结束值
      perf_stop();


      check_result(data);
      return;
  }


  __device__ void loop_unroll_count_2()
  {
      int data[SIZE];
      init_data(data);


      // 收集性能数据起始值
      perf_start();
      for (int i = 0; i < N; i++) {
          #pragma clang loop unroll_count(2)
          for (int j = 0; j < SIZE; j++) {
                data[j] = data[j] + 1;
          }
      }
      // 收集性能数据结束值
      perf_stop();


      check_result(data);
      return;
  }


  __device__ void loop_unroll_count_3()
  {
      int data[SIZE];
      init_data(data);


      // 收集性能数据起始值
      perf_start();
      for (int i = 0; i < N; i++) {
          #pragma clang loop unroll_count(3)
          for (int j = 0; j < SIZE; j++) {
                data[j] = data[j] + 1;
          }
      }
      // 收集性能数据结束值
      perf_stop();


      check_result(data);
      return;
  }


  __device__ void loop_unroll_count_4()
  {
      int data[SIZE];
      init_data(data);


      // 收集性能数据起始值
      perf_start();
      for (int i = 0; i < N; i++) {
          #pragma clang loop unroll_count(4)
          for (int j = 0; j < SIZE; j++) {
                data[j] = data[j] + 1;
          }
      }
      // 收集性能数据结束值
      perf_stop();


      check_result(data);
      return;
  }


  __device__ void loop_unroll_count_5()
  {
      int data[SIZE];
      init_data(data);


      // 收集性能数据起始值
      perf_start();
      for (int i = 0; i < N; i++) {
          #pragma clang loop unroll_count(5)
          for (int j = 0; j < SIZE; j++) {
                data[j] = data[j] + 1;
          }
      }
      // 收集性能数据结束值
      perf_stop();


      check_result(data);
      return;
  }


  __device__ void loop_unroll_count_6()
  {
      int data[SIZE];
      init_data(data);


      // 收集性能数据起始值
      perf_start();
      for (int i = 0; i < N; i++) {
          #pragma clang loop unroll_count(6)
          for (int j = 0; j < SIZE; j++) {
                data[j] = data[j] + 1;
          }
      }
      // 收集性能数据结束值
      perf_stop();


      check_result(data);
      return;
  }


  __device__ void loop_unroll_count_7()
  {
      int data[SIZE];
      init_data(data);


      // 收集性能数据起始值
      perf_start();
      for (int i = 0; i < N; i++) {
          #pragma clang loop unroll_count(7)
          for (int j = 0; j < SIZE; j++) {
                data[j] = data[j] + 1;
          }
      }
      // 收集性能数据结束值
      perf_stop();


      check_result(data);
      return;
  }


  __device__ void loop_unroll_count_8()
  {
      int data[SIZE];
      init_data(data);


      // 收集性能数据起始值
      perf_start();
      for (int i = 0; i < N; i++) {
          #pragma clang loop unroll_count(8)
          for (int j = 0; j < SIZE; j++) {
                data[j] = data[j] + 1;
          }
      }
      // 收集性能数据结束值
      perf_stop();


      check_result(data);
      return;
  }


  __device__ void loop_unroll_count_9()
  {
      int data[SIZE];
      init_data(data);


      // 收集性能数据起始值
      perf_start();
      for (int i = 0; i < N; i++) {
          #pragma clang loop unroll_count(9)
          for (int j = 0; j < SIZE; j++) {
                data[j] = data[j] + 1;
          }
      }
      // 收集性能数据结束值
      perf_stop();


      check_result(data);
      return;
  }


  __device__ void loop_unroll_count_10()
  {
      int data[SIZE];
      init_data(data);


      // 收集性能数据起始值
      perf_start();
      for (int i = 0; i < N; i++) {
          #pragma clang loop unroll_count(10)
          for (int j = 0; j < SIZE; j++) {
                data[j] = data[j] + 1;
          }
      }
      // 收集性能数据结束值
      perf_stop();


      check_result(data);
      return;
  }


  __global__ void call_kernel()
  {
      if (threadIdx != 0) {
          return;
      }
      loop_unroll_do_nothing();
      loop_unroll_enable();
      loop_unroll_disable();
      loop_unroll_full();
      loop_unroll_count_1();
      loop_unroll_count_2();
      loop_unroll_count_3();
      // 输出性能数据
      perf_print();
      loop_unroll_count_4();
      loop_unroll_count_5();
      loop_unroll_count_6();
      loop_unroll_count_7();
      loop_unroll_count_8();
      loop_unroll_count_9();
      loop_unroll_count_10();
      // 输出性能数据
      perf_print();
      return;
  }


  int main()
  {
      // 设置使用2号SPA
      sdaaSetDevice(0);
      // kernel函数调用
      call_kernel<<<1>>>();
      // 等待kernel函数执行完成
      sdaaDeviceSynchronize();
      return 0;
  }
```

#### 优化效果

| 编译指示 | 平均每次运算的执行周期（拍） |
|---|---|
| 无 | 9016 |
| `#pragma clang loop_unroll_enable` | 2840 |
| `#pragma clang loop_unroll_disable` | 9016 |
| `#pragma clang loop_unroll_full` | 2840 |
| `#pragma clang loop unroll_count(1)` | 9016 |
| `#pragma clang loop unroll_count(2)` | 6016 |
| `#pragma clang loop unroll_count(3)` | 3688 |
| `#pragma clang loop unroll_count(4)` | 3515 |
| `#pragma clang loop unroll_count(5)` | 2816 |
| `#pragma clang loop unroll_count(6)` | 5164 |
| `#pragma clang loop unroll_count(7)` | 4564 |
| `#pragma clang loop unroll_count(8)` | 2514 |
| `#pragma clang loop unroll_count(9)` | 3019 |
| `#pragma clang loop unroll_count(10)` | 3016 |

在本示例中，循环展开 8 次时的效果最佳，性能是不做循环展开的约 3.58 倍。

### 4.4.2 链接时优化（LTO）

本文主要介绍使用链接时优化（LTO）提高性能的优化策略和示例。

#### 原理介绍

链接时优化（LTO）是对整个 SDAA C 程序在链接时进行的跨模块间优化，聚焦于分析整个程序代码，属于过程间优化（IPO）的一种。

具体实现原理是将一些 pass 的优化推迟到链接时再进行，从而使得这些 pass 具有可跨模块优化的能力。

LTO 可以实现：

- 更好的内联：LTO 可以在链接阶段分析整个程序的调用关系，从而实现智能的函数内联决策，提高程序性能。
- 消除死代码：LTO 可以分析整个程序的数据流和控制流，检测并删除不会被执行的代码，从而减小程序大小。
- 传播跨模块的常量：LTO 可以在整个程序范围内传播已知的常量值，从而提高程序性能。
- 消除跨模块的重复代码：LTO 可以在整个程序范围内检测并消除重复的代码片段，从而减小程序大小。

#### 优化策略

通过以下编译选项，控制是否需启用 LTO 优化：

- `-flto` ：启用 LTO 优化，编译和链接时使用。
- `--sdaa-link` ：启用 LTO 优化，链接时使用，指示链接 SDAA C 的 `clang-offload-bundler` 捆绑包。

#### 优化示例

分别使用以下两种方式编译示例程序，测试执行周期并比较：

- 不使用 LTO 优化
- 使用 LTO 优化

每个测试均调用 10000 次 `foo1` 函数并计算平均值以消除初次调用时 cache 影响。

##### 不使用 LTO 优化

执行以下编译命令，将 `lib.scpp` 编译成静态库，并使用 `main.scpp` 链接该静态库生成可执行文件：

```bash
  # 将lib.scpp编译生成.o
  tecocc lib.scpp --sdaa-device-only -c -o nonlto.o
  # 将生成的.o打包成静态库
  ar -rcs nonlto.a nonlto.o
  # 编译main.scpp并链接静态库nonlto.a生成可执行文件
  tecocc main.scpp --sdaa-static-device-lib=nonlto.a -o nonlto.out
```

##### 使用 LTO 优化

执行以下编译命令，将 `lib.scpp` 编译成静态库，并使用 `main.scpp` 链接该静态库生成可执行文件：

```bash
  # 将lib.scpp编译生成.o
  tecocc lib.scpp --sdaa-device-only -flto -c -o lto.o
  # 将生成的.o打包成静态库
  ar -rcs lto.a lto.o
  # 编译main.scpp并链接静态库lto.a生成可执行文件
  tecocc main.scpp --sdaa-static-device-lib=lto.a -flto --sdaa-link -o lto.out
```

#### 测试环境

| TecoDriver 和 TecoToolKit 版本 | 计算核心频率 |
|---|---|
| v1.2.0 | 2.36GHz |

#### 测试代码

```cpp
  // main.scpp
  #include <sdaa_perf.h>
  using namespace sdaa;


  #define N 10000


  extern __device__ int foo1();


  __device__ void foo4()
  {
          printf("this is foo4\n");
  }


  __device__ void call_foo1()
  {
          int retval;
          // 收集性能数据起始值
          perf_start();
          for (int i = 0; i < N; i++) {
              retval = foo1();
          }
          // 收集性能数据结束值
          perf_stop();
          // 输出性能数据
          perf_print();
          if (retval != 42) {
              printf("failed!\n");
          } else {
              printf("pass!\n");
          }
  }


  __global__ void call_kernel()
  {
          // 仅使用0号SPE
          if (threadIdx != 0) {
              return;
          }
          call_foo1();
          return;
  }


  int main()
  {
          // 设置使用0号SPA
          sdaaSetDevice(0);
          // kernel函数调用
          call_kernel<<<1>>>();
          // 等待kernel函数执行完成
          sdaaDeviceSynchronize();
      return 0;
  }


  // lib.scpp
  extern __device__ void foo4();


  __device__    int val = 0;


  __device__ void foo2()
  {
      val = -1;
  }


  __device__ int foo3()
  {
      foo4();
      return 10;
  }


  __device__ int foo1()
  {
      int data = 0;
      if (val < 0) {
          data = foo3();
      }
      data += 42;
      return data;
  }
```

#### 优化效果

| 是否使用 LTO 优化 | 平均每次运算的执行周期（拍） |
|---|---|
| 使用 | 341 |
| 不使用 | 707 |

在本示例中，使用 LTO 优化的性能是不使用 LTO 优化的性能约 2.07 倍。

### 4.4.3 浮点融合

本文主要介绍使用浮点融合提高性能的优化策略和示例。

#### 原理介绍

浮点融合是使用一条乘加/乘减运算操作代替乘法和加/减法两条运算的操作，从而提高程序性能并减小误差。

#### 优化策略

通过以下编译选项控制是否需启用浮点融合功能：

- `-ffp-contract=fast` ：启用浮点融合，忽略 `#pragma FP_CONTRACT` 。
- `-ffp-contract=on` ：根据 `#pragma FP_CONTRACT` 决定是否启用浮点融合。
- `-ffp-contract=off` ：禁用浮点融合，忽略 `#pragma FP_CONTRACT` 。

默认为启用浮点融合，您可以调整浮点运算写法，拼凑尽量多的乘加类操作，帮助编译器优化。

目前支持以下几种操作的融合：

- 单精度乘加（单精度同号相乘再做加法）
- 双精度乘加（双精度同号相乘再做加法）
- 单精度乘减（单精度同号相乘再做减法）
- 双精度乘减（双精度同号相乘再做减法）
- 单精度负乘加（单精度异号相乘再做加法）
- 双精度负乘加（双精度异号相乘再做加法）
- 单精度负乘减（单精度异号相乘再做减法）
- 双精度负乘减（双精度异号相乘再做减法）

#### 优化示例

使用不同的编译选项，在 for 循环内进行 100 次乘法和加/减操作，测试以下情况的执行周期并比较：

- 单精度乘加
- 双精度乘加
- 单精度乘减
- 双精度乘减
- 单精度负乘加
- 双精度负乘加
- 单精度负乘减
- 双精度负乘减

每个测例均调用 10000 次乘法和加/减操作计算平均值以消除初次调用时 cache 影响。

#### 测试环境

| TecoDriver 和 TecoToolKit 版本 | 计算核心频率 |
|---|---|
| v1.2.0 | 2.36GHz |

#### 测试代码

```cpp
  #include <sdaa_perf.h>
  using namespace sdaa;


  #define N 10000
  #define SIZE 100
  #define NUM 4
  #define MAX 200
  #define MIN 10


  __global__ void test_fmas(float *data)
  {
      if (threadIdx != 0) {
          return;
      }
      // 收集性能数据起始值
      perf_start();
      for (int i = 0; i < N; i++) {
          for (int j = 0; j < SIZE; j++) {
              data[0 * SIZE + j] = data[1 * SIZE + j] * data[2 * SIZE +j] +
                                   data[3 * SIZE + j];
          }
      }
      // 收集性能数据结束值
      perf_stop();
      // 输出性能数据
      perf_print();
  }


  __global__ void test_fmss(float *data)
  {
      if (threadIdx != 0) {
          return;
      }
      // 收集性能数据起始值
      perf_start();
      for (int i = 0; i < N; i++) {
          for (int j = 0; j < SIZE; j++) {
              data[0 * SIZE + j] = data[1 * SIZE + j] * data[2 * SIZE +j] -
                                   data[3 * SIZE + j];
          }
      }
      // 收集性能数据结束值
      perf_stop();
      // 输出性能数据
      perf_print();
  }


  __global__ void test_fnmas(float *data)
  {
      if (threadIdx != 0) {
          return;
      }
      // 收集性能数据起始值
      perf_start();
      for (int i = 0; i < N; i++) {
          for (int j = 0; j < SIZE; j++) {
              data[0 * SIZE + j] = -((data[1 * SIZE + j]) * data[2 * SIZE + j]) +
                                   data[3 * SIZE + j];
          }
      }
      // 收集性能数据结束值
      perf_stop();
      // 输出性能数据
      perf_print();
  }


  __global__ void test_fnmss(float *data)
  {
      if (threadIdx != 0) {
          return;
      }
      // 收集性能数据起始值
      perf_start();
      for (int i = 0; i < N; i++) {
          for (int j = 0; j < SIZE; j++) {
              data[0 * SIZE + j] = -((data[1 * SIZE + j]) * data[2 * SIZE + j]) -
                                   data[3 * SIZE + j];
          }
      }
      // 收集性能数据结束值
      perf_stop();
      // 输出性能数据
      perf_print();
  }


  __global__ void test_fmad(double *data)
  {
      if (threadIdx != 0) {
          return;
      }
      // 收集性能数据起始值
      perf_start();
      for (int i = 0; i < N; i++) {
          for (int j = 0; j < SIZE; j++) {
              data[0 * SIZE + j] = data[1 * SIZE + j] * data[2 * SIZE +j] +
                                   data[3 * SIZE + j];
          }
      }
      // 收集性能数据结束值
      perf_stop();
      // 输出性能数据
      perf_print();
  }


  __global__ void test_fmsd(double *data)
  {
      if (threadIdx != 0) {
          return;
      }
      // 收集性能数据起始值
      perf_start();
      for (int i = 0; i < N; i++) {
          for (int j = 0; j < SIZE; j++) {
              data[0 * SIZE + j] = data[1 * SIZE + j] * data[2 * SIZE +j] -
                                   data[3 * SIZE + j];
          }
      }
      // 收集性能数据结束值
      perf_stop();
      // 输出性能数据
      perf_print();
  }


  __global__ void test_fnmad(double *data)
  {
      if (threadIdx != 0) {
          return;
      }
      // 收集性能数据起始值
      perf_start();
      for (int i = 0; i < N; i++) {
          for (int j = 0; j < SIZE; j++) {
              data[0 * SIZE + j] = -((data[1 * SIZE + j]) * data[2 * SIZE + j]) +
                                   data[3 * SIZE + j];
          }
      }
      // 收集性能数据结束值
      perf_stop();
      // 输出性能数据
      perf_print();
  }


  __global__ void test_fnmsd(double *data)
  {
      if (threadIdx != 0) {
          return;
      }
      // 收集性能数据起始值
      perf_start();
      for (int i = 0; i < N; i++) {
          for (int j = 0; j < SIZE; j++) {
              data[0 * SIZE + j] = -((data[1 * SIZE + j]) * data[2 * SIZE + j]) -
                                   data[3 * SIZE + j];
          }
      }
      // 收集性能数据结束值
      perf_stop();
      // 输出性能数据
      perf_print();
  }


  void call_kernel_float(float *src, float (*output)[SIZE])
  {
      float *dev_fmas = NULL;
      float *dev_fmss = NULL;
      float *dev_fnmas = NULL;
      float *dev_fnmss = NULL;
      // 设置使用0号SPA
      sdaaSetDevice(0);
      // 在Global内存中malloc一段空间
      sdaaMalloc((void **)(&dev_fmas), NUM * SIZE * sizeof(float));
      // 将数据从Host端拷贝到Device端
      sdaaMemcpy(dev_fmas, src, NUM * SIZE * sizeof(float),
                 sdaaMemcpyHostToDevice);
      // 在Global内存中malloc一段空间
      sdaaMalloc((void **)(&dev_fmss), NUM * SIZE * sizeof(float));
      // 将数据从Host端拷贝到Device端
      sdaaMemcpy(dev_fmss, src, NUM * SIZE * sizeof(float),
                 sdaaMemcpyHostToDevice);
      // 在Global内存中malloc一段空间
      sdaaMalloc((void **)(&dev_fnmas), NUM * SIZE * sizeof(float));
      // 将数据从Host端拷贝到Device端
      sdaaMemcpy(dev_fnmas, src, NUM * SIZE * sizeof(float),
                 sdaaMemcpyHostToDevice);
      // 在Global内存中malloc一段空间
      sdaaMalloc((void **)(&dev_fnmss), NUM * SIZE * sizeof(float));
      // 将数据从Host端拷贝到Device端
      sdaaMemcpy(dev_fnmss, src, NUM * SIZE * sizeof(float),
                 sdaaMemcpyHostToDevice);


      test_fmas<<<1>>>(dev_fmas);
      test_fmss<<<1>>>(dev_fmss);
      test_fnmas<<<1>>>(dev_fnmas);
      test_fnmss<<<1>>>(dev_fnmss);


      // 将数据从Device端拷贝到Host端
      sdaaMemcpy(output[0], dev_fmas, SIZE * sizeof(float),
                 sdaaMemcpyDeviceToHost);
      sdaaMemcpy(output[1], dev_fmss, SIZE * sizeof(float),
                 sdaaMemcpyDeviceToHost);
      sdaaMemcpy(output[2], dev_fnmas, SIZE * sizeof(float),
                 sdaaMemcpyDeviceToHost);
      sdaaMemcpy(output[3], dev_fnmss, SIZE * sizeof(float),
                 sdaaMemcpyDeviceToHost);


      // 释放申请的Global内存
      sdaaFree(dev_fmas);
      sdaaFree(dev_fmss);
      sdaaFree(dev_fnmas);
      sdaaFree(dev_fnmss);
  }


  void call_kernel_double(double *src, double (*output)[SIZE])
  {
      double *dev_fmad = NULL;
      double *dev_fmsd = NULL;
      double *dev_fnmad = NULL;
      double *dev_fnmsd = NULL;
      // 设置使用0号SPA
      sdaaSetDevice(0);
      // 在Global内存中malloc一段空间
      sdaaMalloc((void **)(&dev_fmad), NUM * SIZE * sizeof(double));
      // 将数据从Host端拷贝到Device端
      sdaaMemcpy(dev_fmad, src, NUM * SIZE * sizeof(double),
                 sdaaMemcpyHostToDevice);
      // 在Global内存中malloc一段空间
      sdaaMalloc((void **)(&dev_fmsd), NUM * SIZE * sizeof(double));
      // 将数据从Host端拷贝到Device端
      sdaaMemcpy(dev_fmsd, src, NUM * SIZE * sizeof(double),
                 sdaaMemcpyHostToDevice);
      // 在Global内存中malloc一段空间
      sdaaMalloc((void **)(&dev_fnmad), NUM * SIZE * sizeof(double));
      // 将数据从Host端拷贝到Device端
      sdaaMemcpy(dev_fnmad, src, NUM * SIZE * sizeof(double),
                 sdaaMemcpyHostToDevice);
      // 在Global内存中malloc一段空间
      sdaaMalloc((void **)(&dev_fnmsd), NUM * SIZE * sizeof(double));
      // 将数据从Host端拷贝到Device端
      sdaaMemcpy(dev_fnmsd, src, NUM * SIZE * sizeof(double),
                 sdaaMemcpyHostToDevice);


      // kernel函数调用
      test_fmad<<<1>>>(dev_fmad);
      test_fmsd<<<1>>>(dev_fmsd);
      test_fnmad<<<1>>>(dev_fnmad);
      test_fnmsd<<<1>>>(dev_fnmsd);


      // 将数据从Device端拷贝到Host端
      sdaaMemcpy(output[0], dev_fmad, SIZE * sizeof(double),
                 sdaaMemcpyDeviceToHost);
      sdaaMemcpy(output[1], dev_fmsd, SIZE * sizeof(double),
                 sdaaMemcpyDeviceToHost);
      sdaaMemcpy(output[2], dev_fnmad, SIZE * sizeof(double),
                 sdaaMemcpyDeviceToHost);
      sdaaMemcpy(output[3], dev_fnmsd, SIZE * sizeof(double),
                 sdaaMemcpyDeviceToHost);


      // 释放申请的Global内存
      sdaaFree(dev_fmad);
      sdaaFree(dev_fmsd);
      sdaaFree(dev_fnmad);
      sdaaFree(dev_fnmsd);
  }


  void test_float()
  {
      int errnum = 0;
      float data[NUM * SIZE];
      float dev_output[NUM][SIZE];
      float host_output[NUM][SIZE];


      // init data
      for (int i = 0; i < NUM * SIZE; i++) {
          data[i] = (((float)rand()) / (float)RAND_MAX) * (MAX - MIN) + MIN;
      }
      for (int i = 0; i < NUM; i++) {
          for (int j = 0; j < SIZE; j++) {
              dev_output[i][j] = 0;
              host_output[i][j] = 0;
          }
      }
      // calculate in host
      for (int i = 0; i < SIZE; i++) {
          host_output[0][i] = data[1 * SIZE + i] * data[2 * SIZE + i] +
                                data[3 * SIZE + i];
          host_output[1][i] = data[1 * SIZE + i] * data[2 * SIZE + i] -
                                data[3 * SIZE + i];
          host_output[2][i] = -(data[1 * SIZE + i] * data[2 * SIZE + i]) +
                                data[3 * SIZE + i];
          host_output[3][i] = -(data[1 * SIZE + i] * data[2 * SIZE + i]) -
                                data[3 * SIZE + i];
      }


      call_kernel_float(data, dev_output);


      for (int i= 0; i < NUM; i++) {
          for (int j = 0; j < SIZE; j++) {
              if (fabs(host_output[i][j] - dev_output[i][j]) > 1e-2) {
                     printf("host_output[%d][%d] = %f dev_output[%d][%d] = %f\n",
                            i, j, host_output[i][j], i, j, dev_output[i][j]);
                     errnum++;
              }
          }
      }


      if (!errnum) {
          printf("all pass in float!\n");
      } else {
          printf("error in float!\n");
      }
  }


  void test_double()
  {
      int errnum = 0;
      double data[NUM * SIZE];
      double dev_output[NUM][SIZE];
      double host_output[NUM][SIZE];


      // init data
      for (int i = 0; i < NUM * SIZE; i++) {
          data[i] = (((double)rand()) / (double)RAND_MAX) * (MAX - MIN) + MIN;
      }
      for (int i = 0; i < NUM; i++) {
          for (int j = 0; j < SIZE; j++) {
              dev_output[i][j] = 0;
              host_output[i][j] = 0;
          }
      }
      // calculate in host
      for (int i = 0; i < SIZE; i++) {
          host_output[0][i] = data[1 * SIZE + i] * data[2 * SIZE + i] +
                                 data[3 * SIZE + i];
          host_output[1][i] = data[1 * SIZE + i] * data[2 * SIZE + i] -
                                 data[3 * SIZE + i];
          host_output[2][i] = -(data[1 * SIZE + i] * data[2 * SIZE + i]) +
                                 data[3 * SIZE + i];
          host_output[3][i] = -(data[1 * SIZE + i] * data[2 * SIZE + i]) -
                                 data[3 * SIZE + i];
      }


      call_kernel_double(data, dev_output);


      for (int i= 0; i < NUM; i++) {
          for (int j = 0; j < SIZE; j++) {
              if (fabs(host_output[i][j] - dev_output[i][j]) > 1e-9) {
                     printf("host_output[%d][%d] = %lf dev_output[%d][%d] = %lf\n",
                            i, j, host_output[i][j], i, j, dev_output[i][j]);
                     errnum++;
              }
          }
      }


      if (!errnum) {
           printf("all pass in double!\n");
      } else {
           printf("error in double!\n");
      }
  }


  int main()
  {
      test_float();
      test_double();
      return 0;
  }
```

#### 优化效果

| 编译选项 | 运算类型 | 平均每次运算的执行周期（拍） |
|---|---|---|
| -ffp-contract=fast | 单精度乘加 | 35389 |
| -ffp-contract=fast | 单精度乘减 | 35421 |
| -ffp-contract=fast | 单精度负乘加 | 35389 |
| -ffp-contract=fast | 单精度负乘减 | 35392 |
| -ffp-contract=fast | 双精度乘加 | 35738 |
| -ffp-contract=fast | 双精度乘减 | 35851 |
| -ffp-contract=fast | 双精度负乘加 | 35742 |
| -ffp-contract=fast | 双精度负乘减 | 35856 |
| -ffp-contract=on | 单精度乘加 | 69008 |
| -ffp-contract=on | 单精度乘减 | 69003 |
| -ffp-contract=on | 单精度负乘加 | 69008 |
| -ffp-contract=on | 单精度负乘减 | 69009 |
| -ffp-contract=on | 双精度乘加 | 69752 |
| -ffp-contract=on | 双精度乘减 | 69751 |
| -ffp-contract=on | 双精度负乘加 | 69721 |
| -ffp-contract=on | 双精度负乘减 | 69852 |
| -ffp-contract=off | 单精度乘加 | 69008 |
| -ffp-contract=off | 单精度乘减 | 69009 |
| -ffp-contract=off | 单精度负乘加 | 69008 |
| -ffp-contract=off | 单精度负乘减 | 69007 |
| -ffp-contract=off | 双精度乘加 | 69728 |
| -ffp-contract=off | 双精度乘减 | 69727 |
| -ffp-contract=off | 双精度负乘加 | 69756 |
| -ffp-contract=off | 双精度负乘减 | 69847 |

在本示例中，使用浮点融合的性能是不使用浮点融合的性能约 1.95 倍，不同的数据的类型与不同的操作类型之间性能差异不大。
