---
id: doc-sdaa-c-getting-started-v1-1-0-g01
title: "SDAA C 零基础入门 v1.1.0 — 第g01部分 (ch1)"
type: source-doc-part
parent_doc: doc-sdaa-c-getting-started-v1-1-0
product_version: "v1.1.0"
source_file: "external/文档/SDAA C零基础入门_v1.1.0.pdf"
raw_text: "sources/docs/raw/SDAA C零基础入门_v1.1.0.txt"
raw_line_range: "158-285"
architectures: [sdaa, teco-t1]
confidence: verified
tags: [gemm, large-model, matmul, migration, programming-model, sdaa, teco-t1, tutorial]
---

# SDAA C 零基础入门 v1.1.0（第g01部分）

## 1. 概述

### 1.1 什么是算子开发

算子（Operator）通常是指在深度学习、机器学习或其他数值计算领域中，用于执行特定数学操作的函数，例如：加法、矩阵乘、卷积等。

算子是模型训练推理的重要组成部分，主要用于构建、部署深度学习神经网络模型等。一个高性能的算子可以提升模型的性能，加速模型的训练和推理。

在以下场景，需要进行算子开发：

- 训练场景下，将模型迁移到太初 AI 加速卡上，存在不支持的算子。
- 推理场景下，将模型转换为太初推理框架支持的模型时，存在不支持的算子。
- 提升模型性能，需要开发更高性能的算子。

### 1.2 算子基本概念

**算子类型（Type）**

算子类型用于描述实现逻辑相同的一类算子，例如：卷积类算子、池化类算子等。

> 说明：
>
> - 在一个神经网络中，同一类型的算子可能会存在多个，例如：在某个神经网络中，存在 2 个卷积类算子 conv1，conv2。
> - 同一类型算子的实现可能会存在差别，例如：conv1，conv2 都属于卷积类算子，但实现会根据具体的使用情况而不同。

**算子名称（Name）**

神经网络中具体算子的名称，算子名称在同一神经网络中需要保持唯一。

**算子数据结构（Tensor）**

张量（Tensor）是存储算子输入数据和输出数据的基本数据结构。张量的属性主要包括：名称（name）、形状（shape）、数据类型（dtype）和数据排布格式（format）。

**张量名称（name）**

张量的名称，可以根据名称对张量进行索引。

**张量形状（shape）**

张量的形状用于描述张量的维度及其大小，表述形式：(S1, S2, …SN)，其中 SN 是正整数。例如：

- **(6)**：有 6 个正整数的一维张量即向量，如：(1,2,3,4,5,6)
- **(2,3)**：有 2 个维度的张量，右起第一个维度矩阵宽度（W）是 3，第二个维度矩阵高度（H）是 2，是一个 2 × 3 的矩阵，如：

$$
\begin{pmatrix}
1 & 2 & 3 \\
4 & 5 & 6
\end{pmatrix}
$$

- **(3,2,3)**：有 3 个维度的张量，右起第一个维度矩阵宽度（W）是 3，第二个维度矩阵高度（H）是 2，第三个维度矩阵个数（N）是 3，表示 3 个 2 × 3 矩阵

图1 张量形状

**张量的数据类型（dtype）**

张量中存储的数据的类型，取值范围：int8、int16、float16、float32 等。

**张量的数据排布格式（format）**

在内存中张量数据的存储格式，不同的数据排布格式会影响数据的访问模式、内存利用率和算子的性能。

以四维张量 (N, H, W, C) 为例，四个维度结合图像方面的物理含义如下：

- **N（Batch）**：一个批次内图片的数量，即一次处理的图片数量。
- **H（Height）**：特征图的高，即垂直高度方向的像素个数。
- **W（Width）**：特征图的宽，即水平宽度方向的像素个数。
- **C（Channels）**：特征图的通道数，例如：灰度图像为通道数 1，彩色 RGB 图像为 3。

由于内存中只能进行线性存储，因此对于四维张量需要根据不同的顺序进行存储，例如：

图2 张量的数据排布格式

### 1.3 SDAA 编程模型

#### 1.3.1 SDAA 异构并行计算平台

SDAA C（Software Defined Accelerator Architecture C）是运行在 SDAA 异构并行计算平台上的编程模型。以太初 AI 加速卡为计算核心，支持 C/C++ 编程语言，您仅需学习少量的 SDAA C 编程语法即可快速上手编程。同时 SDAA C 最大化地隐藏了底层硬件的实现细节，用户无需深入了解硬件的体系架构就可以正常使用太初 AI 加速卡提供的强大算力，大幅度地降低学习成本，以便您可以专注于并行程序的开发。SDAA C 的详细信息，可参考《SDAA C 编程指南》。
